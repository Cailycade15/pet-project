// Технический идентификатор "все категории". Он используется только
// для логики фильтрации и не должен зависеть от языка интерфейса —
// видимый пользователю текст берётся из переводов (filters.allCategories).
export const ALL_CATEGORIES = "__all_categories__";
